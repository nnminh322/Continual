# GainLoRA extra dependencies
# ============================
# Colab/Kaggle already provides: torch, transformers, accelerate, datasets,
# peft, tokenizers, safetensors, huggingface-hub, scikit-learn, scipy, nltk,
# sentencepiece, regex, pyyaml, tqdm, click, pydantic, absl-py, fsspec, dill
#
# This file installs ONLY what is genuinely missing from Colab/Kaggle.
# Do NOT pin versions of packages already in Colab — it downgrades them
# and breaks peft/accelerate compatibility.
#
# Installation (Colab/Kaggle):
#   !pip install -r requirements.txt
#
# Installation (bare GPU server):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#   pip install transformers accelerate datasets peft
#   pip install -r requirements.txt

# LoRA helper layers
loralib

# Training optimization
deepspeed
optimum

# Profiling / FLOPs counting
ptflops
thop
calflops

# GPU monitoring
pynvml

# Utilities
tabulate
pandas

# Debugging
ipdb

# cupy (optional, for SVD-based GPM — install manually if needed)
# pip install cupy-cuda12x   # for CUDA 12.x
# pip install cupy-cuda11x   # for CUDA 11.x
