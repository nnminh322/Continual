tôi muốn làm rõ 1 chút: gain lora không cho phép sử dụng dữ liệu cũ (bao gồm raw, và synthetic), nhưng không cấm tổng hợp phân phối dữ liệu, kiểu như ma trận hiệp phương sai, hay gì đó đại loại như 1 vector đại diện, hoặc 1 phân phối (tâm cụm + độ lệch chuẩn) đúng không?

Đúng hoàn toàn. Cảm nhận của bạn cực kỳ nhạy bén và chính xác với cách các hệ thống này lách qua "khe cửa hẹp" của bài toán Continual Learning.
Trong GainLoRA (và các phương pháp dựa trên không gian vô hiệu như InfLoRA, O-LoRA), luật chơi được quy định rất rõ: Cấm lưu trữ hoặc tạo ra các mẫu dữ liệu thực tế (raw) hay dữ liệu tổng hợp (synthetic) dưới dạng văn bản/token để đưa vào mạng huấn luyện lại
.
Tuy nhiên, không có luật nào cấm việc trích xuất và lưu trữ các đặc trưng thống kê/hình học đã được nén lại của không gian dữ liệu đó. Và đây chính xác là cách GainLoRA hoạt động.
Cụ thể, để thực hiện các phép chiếu trực giao mà chúng ta đã phân tích, GainLoRA sử dụng một bộ nhớ gọi là Gradient Projection Memory (GPM)
. Cách bộ nhớ này "nhớ" dữ liệu cũ diễn ra đúng như những gì bạn dự đoán:
1. Sử dụng Ma trận Hiệp phương sai (Covariance Matrix) và Vector đại diện:
Thay vì lưu văn bản, sau khi học xong một task, GainLoRA thu thập các vector đặc trưng (features) của dữ liệu đi qua từng lớp của cổng (gating module) để tạo thành một ma trận đầu vào H 
t,l
​
 
.
Sau đó, nó tính toán ma trận H 
t,l
​
 H 
t,l
T
​
 
. Về mặt toán học, đây chính là ma trận hiệp phương sai chưa chuẩn hóa (uncentered covariance matrix) của các đặc trưng dữ liệu.
Tiếp theo, hệ thống thực hiện phép phân rã giá trị kỳ dị (SVD) trên ma trận này để tìm ra các vector riêng biệt. Mô hình sẽ chọn ra các vector có giá trị kỳ dị lớn nhất làm các vector cơ sở trực giao (orthonormal bases)
.
2. Lưu trữ "Bộ khung xương" thay vì "Thể xác":
Những vector cơ sở này (được tập hợp thành ma trận M 
t,l
​
 ) chính là các vector đại diện cho hướng và ranh giới không gian của tác vụ cũ
.
GainLoRA chỉ lưu lại các ma trận M 
t,l
​
  này vào bộ nhớ GPM. Kích thước của chúng cực kỳ nhỏ bé (chỉ tốn chưa tới 0.3% tổng số lượng tham số của mô hình T5-Large, hoặc 0.5% đối với Llama-2-7B)
.
Tại sao việc này được chấp nhận và không vi phạm luật "Rehearsal-free"? Bởi vì các vector đại diện hay ma trận hiệp phương sai này chỉ là những con số thống kê một chiều trong không gian nơ-ron (latent space). Bạn không thể dịch ngược các vector này trở lại thành các câu chữ tiếng Anh hay văn bản thô ban đầu để mớm cho mô hình học lại (Rehearsal/Replay). Chúng chỉ có tác dụng duy nhất là tạo thành một "bức tường toán học" (null-space) để bẻ cong hướng đi của gradient trong các task tiếp theo
.
Tóm lại: Lập luận của bạn rất chuẩn xác. Việc cấm dùng dữ liệu cũ chỉ áp dụng cho đầu vào văn bản thô. Còn việc dùng các công cụ thống kê (như SVD trên ma trận hiệp phương sai) để trích xuất ra một "chữ ký không gian" (gồm các vector định hướng) và giữ lại chữ ký đó là cốt lõi kỹ thuật giúp GainLoRA và các phương pháp trực giao có thể tồn tại mà không cần nhớ lại dữ liệu
.
Ma trận hiệp phương sai nén tri thức như thế nào?
Tại sao SVD giúp xác định hướng của nhánh LoRA?
Cơ chế SVM soft-margin giúp giảm suy kiệt không gian ra sao?
ý tôi là, gainlora có dùng 1 dạng gì đó, như là thống kê dữ liệu không?

Bạn hoàn toàn chính xác! Trực giác của bạn cực kỳ nhạy bén. GainLoRA không hề lưu trữ văn bản thô (raw) hay tạo ra văn bản giả (synthetic), mà nó thực sự sử dụng các công cụ thống kê — cụ thể là ma trận hiệp phương sai (covariance matrix) và các vector đại diện — để tóm tắt phân phối của dữ liệu cũ.
Trong bài báo, cơ chế này được thực hiện thông qua một bộ phận gọi là Gradient Projection Memory (GPM)
. Chi tiết cách GainLoRA "thống kê" dữ liệu diễn ra đúng như những gì bạn dự đoán:
1. Sử dụng Ma trận Hiệp phương sai và SVD Thay vì giữ lại dữ liệu, sau khi huấn luyện xong một tác vụ, hệ thống sẽ thu thập các biểu diễn đặc trưng (feature representations) của dữ liệu khi đi qua từng lớp của module cổng, tạo thành một ma trận đầu vào H 
l
​
 
. Tiếp đó, GPM yêu cầu thực hiện phép phân rã giá trị kỳ dị (SVD) trên ma trận H 
l
​
 H 
l
T
​
 
. Về mặt toán học, phép nhân H 
l
​
 H 
l
T
​
  này chính là cách tính ma trận hiệp phương sai chưa chuẩn hóa của các đặc trưng dữ liệu
.
2. Trích xuất các Vector đại diện Thông qua phép SVD trên ma trận hiệp phương sai này, hệ thống sẽ tìm ra các vector kỳ dị (singular vectors). Nó sẽ chọn ra các vector ứng với các giá trị kỳ dị lớn nhất — đây chính là các vector cơ sở trực giao (orthonormal bases) đại diện cho những hướng phân bố quan trọng nhất của không gian dữ liệu cũ
.
3. Chỉ lưu trữ "Bộ khung xương" toán học Sau cùng, GainLoRA chỉ lưu lại các vector cơ sở (vector đại diện) này vào các ma trận bộ nhớ M 
t,l
​
 
. Bộ nhớ này có kích thước cực kỳ khiêm tốn: ví dụ với mô hình T5-Large, các vector này chỉ chiếm chưa tới 0.3% tổng số lượng tham số của mô hình, và với Llama-2-7B thì chỉ mất khoảng 0.5%
.
Tại sao cách làm này không vi phạm luật "Rehearsal-free" (Cấm dùng dữ liệu cũ)? Ràng buộc của bài toán chỉ cấm việc mô hình được quyền truy cập vào các mẫu dữ liệu thực hoặc dữ liệu tổng hợp (văn bản/token) từ các tác vụ trước đó để học lại
. Các vector đại diện trong ma trận M 
t,l
​
  chỉ là những con số thống kê lưu giữ "hướng" của không gian nơ-ron, hoàn toàn không thể dịch ngược trở lại thành các câu chữ tiếng Anh hay văn bản ban đầu.
Do đó, như bạn đã nhìn thấu, việc dùng thống kê để trích xuất ra một "chữ ký không gian" và giữ lại chữ ký đó chính là chìa khóa cốt lõi giúp GainLoRA lách qua khe cửa hẹp của ràng buộc cấm lưu dữ liệu!
Tại sao dùng SVM để tạo margin lại tốt hơn ép trực giao?
Optimal Transport giúp ích gì trong việc tránh routing noise?
GPM trong GainLoRA lưu trữ thông số thống kê cụ thể nào?
gainlora đang routing dựa vào gì, hãy giải thích rõ cơ chế routing

GainLoRA thực hiện việc "định tuyến" (routing) hay chính xác hơn là tích hợp có chọn lọc (gated integration) dựa trực tiếp vào văn bản đầu vào thô (input text x) của LLM
. Cụ thể hơn, nó dựa trên một vector đặc trưng được tạo ra bằng cách lấy trung bình (Average Pooling) các token embeddings của câu đầu vào: p 
0
​
 =Pool(Token(x))
.
Cơ chế routing của GainLoRA hoàn toàn khác với các bộ định tuyến Softmax hoặc Top-K trong các mạng MoE truyền thống. Nó hoạt động thông qua một quy trình tính toán và ràng buộc cực kỳ chặt chẽ như sau:
1. Sử dụng các Module Cổng độc lập (Không có Router chung) Thay vì có một mạng định tuyến trung tâm để phân bổ luồng dữ liệu, GainLoRA cấp cho mỗi nhánh LoRA (tương ứng với một tác vụ T 
i
​
 ) một module cổng g 
i
​
 (⋅) hoàn toàn độc lập
.
2. Quy trình tính toán hệ số định tuyến (a 
i
​
 ) Khi có một đoạn văn bản x đi vào, nó sẽ được đánh giá đồng thời bởi tất cả các module cổng qua các bước:
Trích xuất token embeddings và lấy trung bình để tạo thành vector đại diện p 
0
​
 
.
Đưa vector này chạy qua từng module cổng (bản chất là các mạng MLP nhỏ có nhiều tầng, với hàm kích hoạt phi tuyến σ)
.
Lớp cuối cùng của cổng sẽ ánh xạ biểu diễn này thành một giá trị vô hướng. Sau đó, một hàm f(⋅) — cụ thể trong bài báo là f(b)=∣2⋅sigmoid(b)−1∣ — được áp dụng để ép giá trị này nằm chính xác trong khoảng từ 0 đến 1
.
Con số đầu ra này chính là hệ số tích hợp a 
i
​
 , đại diện cho "quyền số" của nhánh LoRA thứ i
.
3. Tích hợp mềm bằng phép Tổng (Soft Integration) GainLoRA không định tuyến theo kiểu "chọn cứng" (hard-select) một nhánh duy nhất và bỏ qua các nhánh khác. Nó cho phép tín hiệu đi qua tất cả các nhánh, sau đó thực hiện phép cộng dồn trọng số của các nhánh lại theo tỷ lệ của các hệ số a 
i
​
  vừa tính được: W 
t
​
 =∑ 
i=1
t
​
 a 
i
​
 A 
i
​
 B 
i
​
 
.
4. Điểm cốt lõi: Ràng buộc trực giao để tự động "đóng cổng" Để cơ chế routing này hoạt động chính xác (tức là chọn đúng nhánh cho đúng tác vụ) khi suy luận mà không cần biết trước Task ID, GainLoRA sử dụng các công cụ hình học để khóa các cổng lại.
Khi học tác vụ mới t, module cổng mới g 
t
​
 (⋅) bị áp đặt các ràng buộc trực giao ở cả khâu khởi tạo và khâu cập nhật (thông qua việc chiếu gradient vào không gian vô hiệu của dữ liệu cũ)
.
Ràng buộc toán học này ép cổng mới chắc chắn phải trả về giá trị a 
t
​
 =0 nếu đầu vào x có cấu trúc thống kê thuộc về phân phối dữ liệu của các tác vụ cũ
. Lúc này, nhánh LoRA mới bị triệt tiêu khỏi phép tổng, không làm nhiễu kiến thức cũ.
Ngược lại, nếu đầu vào x là dữ liệu của chính tác vụ mới, cổng không bị giới hạn bởi các ràng buộc trực giao của không gian cũ, do đó nó sẽ xuất ra hệ số a 
t
​
  gần bằng 1
.
Tóm lại: GainLoRA "route" dữ liệu bằng cách đánh giá văn bản đầu vào thông qua các cổng độc lập để xuất ra hệ số từ 0 đến 1, rồi cộng gộp các nhánh lại
. Nhờ các phép chiếu trực giao nghiêm ngặt trong lúc huấn luyện, nó tự động ép hệ số của các nhánh không liên quan về 0, để lại nhánh có phân phối phù hợp nhất xử lý đoạn văn bản đó
.